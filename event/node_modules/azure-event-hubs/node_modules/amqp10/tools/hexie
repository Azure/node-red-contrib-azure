#!/usr/bin/env node

'use strict';
const input = process.argv[2];

let count = 0;
for (let i = 0; i < input.length; i+=2) {
  process.stdout.write(input.slice(i, i + 2) + ' ');
  count++;
  if (count === 4) {
    process.stdout.write('\n');
    count = 0;
  }
}

console.log('\n');

